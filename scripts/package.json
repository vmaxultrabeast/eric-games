{
  "name": "dino-island-story-generator",
  "description": "Standalone script for daily DinoIsland episode generation — runs via GitHub Actions",
  "version": "1.0.0",
  "main": "generate-story.js",
  "scripts": {
    "generate": "node generate-story.js",
    "seed":     "node seed.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@google-cloud/text-to-speech": "^5.7.0",
    "firebase-admin": "^12.0.0"
  },
  "private": true
}
